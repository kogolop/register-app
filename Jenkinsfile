pipeline {
    agent { label 'Pk-Jenkins-Agent1' }
    tools {
        jdk 'Java17'
        maven 'Maven1"
    }
    stages{
        ("Cleanup Workspace"){
            steps {
              CleanWs()

              ]
      stage("Checkout from SCM"){
            steps {
              git branch: 'main', credentialsId: 'github', url: 
            
}
